<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibroPrintDriver"
        tools:targetApi="26">

        <!-- Admin Settings / Test Activity (password protected) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="관리자 설정" />

        <!-- Ladder Game Activity -->
        <activity
            android:name=".LadderGameActivity"
            android:exported="false"
            android:label="사다리 게임" />

        <!-- Web Print Activity — Launcher (desktop mode WebView) -->
        <activity
            android:name=".WebPrintActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LibroPrintDriver.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PrintService - registered in Settings → Print -->
        <service
            android:name=".LibroPrintService"
            android:exported="true"
            android:permission="android.permission.BIND_PRINT_SERVICE">
            <intent-filter>
                <action android:name="android.printservice.PrintService" />
            </intent-filter>
            <meta-data
                android:name="android.print.printservice"
                android:resource="@xml/print_service_info" />
        </service>

        <!-- Auto-configure PrintService after reboot (A40i) -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
